spring:
  config:
    import: "optional:configserver:http://localhost:9999"
  application:
    name: store
  cloud:
    config:
      uri: http://localhost:9999
      name: store
  profiles:
    active: dev
